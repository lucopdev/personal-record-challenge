const errorMessage400 = { message: 'Missing information data.' }
const errorMessage404= { message: 'User not found.' };
const errorMessage409 = { message: 'Name already registered.' }

export default {
  errorMessage400,
  errorMessage404,
  errorMessage409,
}